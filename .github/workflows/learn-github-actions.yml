name: lint-deploy
on: [push]
jobs:
  deploy:
    runs-on: ubuntu-latest
    env:
      NEXT_PUBLIC_SUPABASE_URL: ${{ secrets.NEXT_PUBLIC_SUPABASE_URL }}
      NEXT_PUBLIC_SUPABASE_ANON_KEY: ${{ secrets.NEXT_PUBLIC_SUPABASE_ANON_KEY }}
    steps:
      - uses: actions/checkout@v3
      - uses: actions/setup-node@v3
        with:
          node-version: "16"
      - run: npm i
      - run: npm run lint
      - run: npm run build
      - run: https://api.netlify.com/build_hooks/5c23354f454e1350f8543e78?trigger_branch=main&trigger_title=triggered+by+This+Awesome+Service
  # deploy:
  #   needs: lint
  #   name: "Deploy to Netlify"
  #   runs-on: ubuntu-latest
  #   steps:
  #     - run: npm i
  #     - run: npm
  #     - uses: jsmrcaga/action-netlify-deploy@v1.1.0
  #       with:
  #         NETLIFY_AUTH_TOKEN: 5Hp1atYRwTge85K0r17WKJzrBiI7i47gARzvGj4ekUg
  #         NETLIFY_SITE_ID: 53461f9b-8302-4b9f-ac34-5905cb780591
  #         NETLIFY_DEPLOY_TO_PROD: true
